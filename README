 SIMION GEORGE-VLAD 312CB   
    
    Tema consta in generarea in tabela hash a unor cuvinte (TWord) care sunt aranjate in functie de prima litera (stil dictionar), lungime si frecventa. Mai intai am creat in main un while prin care citeam linie cu linie si in functie de primul cuvant al fiecarei linii (insert sau print), programul le citeste ca pe niste comenzi.
    Prin insert se intelege inserarea unui element de tip TWord in tabela hash, care de asemenea trebuie inserata intr-un TElement care se regaseste in tabela. In functia de citire linie cu linie, preiau cuvintele de dupa insert, cu lungimea mai mare sau egala de 3 si imi generez o lista, prin care imi fac TElemente ce vor urma a fi inserate in tabela hash. In momentul in care inserez TElementele, pentru fiecare element deja inserat, aplic doua sortari pentru TWord-urile ce se regasesc in TElementul inserat (dupa frecventa si lexicografic). Dupa ce am inserat toate TElementele, parcurg tabela hash si sortez pentru fiecare codHash (cifra corespunzatoare cuvintelor cu o prima litera specifica), TElementele dupa lungimea regasita in structura TElement (lungimea unui cuvant).
    Prin comanda print, se intelege afisarea tabelei hash in punctul in care a ajuns citirea linie cu linie. Daca in continuare vom avea insert-uri si dupa inca un print, tabela hash initiala va fi completata cu noile TElemente. In acest caz de print, exista 3 posibilitati: afisarea tabelei cu totul, tabela intr-o anumita zona (in functie de prima litera) si lungimea acestora sau cuvintele cu o anumita frecventa data de comanda print. Am luat ca variabile lungimea, litera si frecventa cautata si am creat cate 2 functii, pentru afisare element si afisare tabela hash. Aceste functii difera prin cea de print fara alt parametru, prin conditiile impuse, detaliate anterior
